"""Reusable, general-purpose utility modules — importable as ``scripts.*``.

Permanent helpers that are not part of the shipped ``hive`` package (the wheel build
excludes this directory) and are not specific to any one benchmark or experiment.
"""
